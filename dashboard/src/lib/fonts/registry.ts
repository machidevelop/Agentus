export const fontRegistry = {} as const;

export type FontKey = string;

export const fontKeys: FontKey[] = [];

export const fontVars = "";

export const fontOptions: { key: FontKey; label: string }[] = [];
